触发器?
一种存储过程，加强对数据的约束，保证数据的一致性与完整性
存储过程？
一段编译好的SQL语句，创建一次，重复调用
索引？
特殊的查询表，加快数据的查询
视图？
虚拟表，与物理表有相同的功能，对视图的修改不影响基本表
事务？
绑在一起一个逻辑工作单元的SQL语句分组，如果一个语句操作，那么整个操作就会失败（回滚）
内存泄漏？
内存使用完后没有清空释放，则下次就不能被使用。
如何删除重复数据？

NULL是什麽意思？
未知的意思，不是空字符串。
主键与外键？
主键是用来定义表达行的字段，主键的值是唯一的，外键是用来建立两个表格之间关系的约束。
